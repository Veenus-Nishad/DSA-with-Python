import numpy as np # or use * method to avoid using np.array
arr=np.array([1,2,3,4])
print(arr)

for x in arr: #jitne no of elements hai utni baar array print hoga
  print(arr)

for x in range(len(arr)): # prints only elements
  print(arr[x])

# 2D array  
twoD_array=([1,2,3],[34,35,36]) #same size must

# if not same size single array of all elements appended into first one

twoD_array2=([32,34,35],[1,23,3])
